{
  "name": "avatarforge",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings"
  },
  "dependencies": {
    "@dicebear/core": "^10.0.1",
    "@dicebear/styles": "^10.0.1",
    "hono": "^4.9.7"
  },
  "devDependencies": {
    "wrangler": "^4.4.0"
  }
}

